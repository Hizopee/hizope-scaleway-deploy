# Ne jamais committer les vrais secrets — seuls les .env.example sont trackés
.env
gaia-vps/.env
shared-vps/.env

# Build statique de CMicrolocks-admin (copié à la main sur le VPS, pas
# versionné ici — source dans le repo Hizopee/CMicrolocks-admin)
shared-vps/admin-website/

# Build statique de LoveList-client (copié à la main sur le VPS, pas
# versionné ici — source dans le repo Hizopee/LoveList-client)
shared-vps/lovelist-website/

# Build statique du site de réservation CMicrolocks, déployé par la CI de
# Hizopee/CMicrolocks-client-web (rsync de dist/ vers le VPS, pas versionné ici)
shared-vps/cmicrolocks-website/

# Build statique de Hizope-pilotage-web (rsync par sa propre CI, pas versionné ici)
shared-vps/hizope-pilotage-website/
